# Database (Neon PostgreSQL)
DATABASE_URL=your_neon_database_url_here

# Session Secret (Generate a random 32+ character string)
SESSION_SECRET=your-super-secret-session-key-minimum-32-characters-long

# Email Configuration
# Choose ONE method: Resend (recommended) OR SMTP

# Option 1: Resend (Recommended - Modern, Reliable, Easy Setup)
# Get your API key from: https://resend.com/api-keys
# Free tier: 100 emails/day, 3,000 emails/month
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=onboarding@resend.dev
# Note: For production, add and verify your domain at https://resend.com/domains
# Then use: your-bank@yourdomain.com

# Option 2: SMTP (Fallback - Used if RESEND_API_KEY is not set)
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@zohomail.com
SMTP_PASSWORD=your-zoho-password-here
SMTP_FROM_NAME=Sterling Capital Bank
SMTP_FROM_EMAIL=noreply@yourdomain.com

# For Zoho Mail (Primary):
# - SMTP_HOST=smtp.zoho.com
# - SMTP_PORT=587
# - SMTP_SECURE=false
# - Use your Zoho email and password
# - Make sure SMTP is enabled in Zoho Mail settings

# For Gmail:
# 1. Enable 2-Factor Authentication
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the App Password as SMTP_PASSWORD
# - SMTP_HOST=smtp.gmail.com

# For other providers:
# - Outlook: smtp.office365.com:587 (SMTP_SECURE=false)
# - Yahoo: smtp.mail.yahoo.com:587 (SMTP_SECURE=false)
# - SendGrid: smtp.sendgrid.net:587 (SMTP_SECURE=false)

# Supabase Configuration (for image uploads/storage)
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Optional: Application URL (for production)
# NEXT_PUBLIC_APP_URL=https://your-domain.vercel.app
